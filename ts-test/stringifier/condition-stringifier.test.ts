import 'mocha';
import chai from 'chai';


const expect = chai.expect;
const should = chai.should();


const unreachableCode = false;
//const parser = new ConditionParser();
//const scope = new Scope(ScopeType.Custom, 'Test');
//const stringifier = new ConditionStringifier();
/*
describe('Rules Engine Tests', ()=>{
  describe('Condition Stringifier Tests', ()=>{
    describe('core/condition/stringifier/condition-stringifier.test', () => {
      it('should stringify simple condition 5 > 6', done => {
        const [remaining, conditionRef] = parser.parse('5 > 6', scope);
        const stringified = stringifier.stringify(conditionRef, scope);
        stringified.should.equal('5 > 6');
        done();
      });
      it('should stringify attribute condition "<<ex data-type=Number>> path.to.something[5] > 6"', done => {
        const [remaining, conditionRef] = parser.parse('<<ex data-type=Number>> path.to.something[5] > 6', scope);
        const stringified = stringifier.stringify(conditionRef, scope);
        stringified.should.equal('<<ex data-type=Number>> path.to.something[5] > 6');
        done();
      });
      it('should stringify attribute condition "6 < path.to.something[5]"', done => {
        const [remaining, conditionRef] = parser.parse('6 < path.to.something[5]', scope);
        const stringified = stringifier.stringify(conditionRef, scope);
        stringified.should.equal('6 < path.to.something[5]');
        done();
      });
    })
  })
})
*/
